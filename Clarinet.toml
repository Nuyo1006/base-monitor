[project]
name = "base-monitor"
authors = []
description = "A decentralized blockchain activity monitoring system"
telemetry = false
requirements = []
[contracts.base-monitor]
path = "contracts/base-monitor.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
